/**
 * Processes a given settings directory
 * @param inputDirectory Directory to process the input for
 * @return merged results
 */
export async function processImports(inputDirectory: string): Promise<any> {
  // TODO:
  return {};
}
